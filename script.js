// Use of "browser payment APIs". Get doc ready for jQuery
/*var stripeAPI = Stripe("pk_test_P7Plzb4azqJyY0AykXyzua5G00pV8ZeRVp");

$(document).ready(function () {
  var id = "";
})
  $.ajax({
    type: "GET",
    url: "https://api.stripe.com/v1/products/${id}",
    datatype: "JSON",
  }).then(function (response) {
    console.log(response);
    confirmationID = response.id.name;
  });
  $("#receipt_id").append(`<p>Confirmation #: ${id} . Please keep for your records.</p>`);
  });
});
*/
